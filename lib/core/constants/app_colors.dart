import 'package:flutter/material.dart';

class AppColors {
  // ── Warm Light Premium Theme ──
  static const Color primary = Color(0xFF5B4CDB);       // Ungu elegan
  static const Color accent = Color(0xFF00B894);         // Hijau mint
  static const Color secondary = Color(0xFF00B894);      // Hijau mint
  static const Color background = Color(0xFFF5F3EF);     // Krem hangat
  static const Color surface = Color(0xFFFFFFFF);         // Putih bersih
  static const Color cardBackground = Color(0xFFFFFFFF);  // Putih + shadow
  static const Color textPrimary = Color(0xFF2D3436);     // Abu gelap
  static const Color textSecondary = Color(0xFF636E72);   // Abu medium
  static const Color error = Color(0xFFE17055);           // Merah hangat

  // ── Extended palette ──
  static const Color border = Color(0xFFE0DDD8);          // Border halus
  static const Color shimmer = Color(0xFFEAE6E1);         // Shimmer/divider
  static const Color navBar = Color(0xFFFFFFFF);           // NavBar putih
  static const Color cardShadow = Color(0x1A000000);       // Shadow halus
  static const Color inputFill = Color(0xFFF0EDE8);        // Input background
  static const Color sheetBackground = Color(0xFFFAF8F5);  // Bottom sheet
}
